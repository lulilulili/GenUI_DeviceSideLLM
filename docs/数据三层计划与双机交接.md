# 数据三层计划与双机交接（2026-09-12）

> 本文是与 Codex 评审对齐后的**当前权威计划**，覆盖 CC 与 GD 两条线。
> 家中开工前先读 §4 清单——git 同步不到的东西都列在那里。

## 1. 数据三层定位

| 层 | 内容 | 位置 | 用途红线 |
|---|---|---|---|
| L0 bootstrap | Codex 模板集：CC 4200 / GD 3636 | 两仓 `feat/3b-datasets` 分支 | 只做训练管线冒烟与工程回归；分数永不对外汇报（同分布回归分，含句式/答案组合记忆） |
| L1 正式训练 | LLM 反写+审校集：CC `training/datasets/cc_train_v1.jsonl` 3086 / GD `data/factory/sft_train_v1.jsonl` 2992 | 两仓 `claude` 分支 | 微调用；待 v1.1 小修与四层去重报告 |
| L2 冻结评测 | 纯人工签字考卷 | GD `evals/frozen/`（58 条 unsupported 本地）+ CC taskspec-20 | 唯一可对外的数字来源；GD 还缺 7 个桶 |

已审计的事实：L1 的 CC 集术语污染仅 10/2265（"单位"类）、updatedAt 类组合措辞自然
（"这数据是几点查的"）；GD 集对 58 条冻结集三层泄漏检查通过。Codex 对其自产模板集的
批评（句式记忆/跨 split 重复 34/17/17/实现语言泄漏）成立，故定位为 L0。

## 2. 执行序（当前进度标记）

| # | 任务 | 谁 | 状态 |
|---|---|---|---|
| P0-1 | 治理合流：cherry-pick Codex 批量筛查器进 claude；L0 打 synthetic_bootstrap 标记；枚举器统一 125 骨架版 | Claude | 待做 |
| P0-2 | CC v1.1：清 10 条"单位"术语 + 修风格后缀标点（"。，谢谢"）+ 重跑 | Claude | 待做 |
| P0-3 | 四层去重工具：check_leakage 扩展（训练集内部近重复 + 答案组合报告 + CC 接入），两套 L1 出健康报告 | Claude | 待做 |
| P1-4 | bootstrap LoRA 冒烟：用 L0 打通 QLoRA→量化→harness 全链 | 用户（脚本待备） | 待做 |
| P1-5 | 冻结集补齐（唯一必须人工）：GD 7 桶各"手写 10 种子→LLM 扩→逐条签"；CC 人工条目扩到 ≥60 | 用户+Claude | 待做 |
| P2-6 | L1-v2 增量：否定/纠正、动作孪生、GD 跨域多能力卡 | Claude | 待做 |
| P2-7 | 决策点 A：真冻结集 × 裸 3b/1.5b 基线 | 用户跑 | 待做 |

裁决记录：CC 双任务分训+二层选择题维持现状；**GD 输出编号化（Codex 提案）暂缓**，
记为决策点 A 备选——基线 key 错误率 >5% 才切换（现有候选注入实测 EXACT 100%（小样本））。

## 3. 仓库分支地图

| 仓 | 分支 | 内容 |
|---|---|---|
| CreateMyCard | `claude` | 演练场 + training 工具链 + L1 数据集 + 设计说明 |
| CreateMyCard | `feat/3b-datasets` | Codex：L0 数据 + build_3b_dataset + playground 批量筛查器 |
| CreateMyCard | `main` / `wide_feature` | 云侧工程原样 / 原有分支 |
| GenUI_DeviceSideLLM | `claude` | genui_v2 全部 + 布局实验室 + 训练工具四件套 + L1 数据集 + 各设计文档 |
| GenUI_DeviceSideLLM | `feat/3b-datasets` | Codex：L0 数据 + workbench 批量筛查器 |

## 4. 家中开工清单（git 之外必须手动同步/重建）

**必拷（U 盘/网盘）：**
1. `GenUI_DeviceSideLLM-main/evals/frozen/frozen_set_v1.jsonl` — 58 条 unsupported 冻结桶（实习生衍生，永不入库）；
2. `GenUI_DeviceSideLLM-main/evals/frozen/intern_distilled_draft.jsonl`、`intern_distilled_report.json`、`intern_used_seed_ids.txt` — 提炼草稿与防泄漏名单；
3. `实习生数据整理/` 或直接拷 Downloads 里的 `02-seed-and-expanded-data.zip`（10MB，SHA256 已核）到家中同名目录再解压。

**家中环境重建：**
```powershell
git clone -b claude https://github.com/lulilulili/GenUI_DeviceSideLLM
git clone -b claude https://github.com/lulilulili/CreateMyCard
pip install -r CreateMyCard/widget_service/requirements.txt   # gallery/训练工具依赖
# Ollama：安装后 ollama pull qwen2.5:3b && setx OLLAMA_ORIGINS "*"
```

**验证环境就绪（三步）：**
```powershell
cd GenUI_DeviceSideLLM && python -m unittest discover -s tests        # 51+ 全绿
cd ..\CreateMyCard && python -m training.eval_harness --gold          # 全指标 1.0
python evals/run_frozen_eval.py --dataset evals/frozen/frozen_set_v1.jsonl --provider mock   # 加载 58 条零错误（需已拷贝冻结集）
```

（可选）同步 Claude 会话与项目记忆：拷 `C:\Users\<你>\.claude\` 至家中同路径；
只拷 `projects\D--06-GenUi-BasedOnCloud\memory\` 亦可凭记忆文件无缝续接。

## 5. 回家后的第一句话

对 Claude 说"按交接文档继续 P0"即可——P0 三项无需人工介入；
你自己的并行任务是 P1-5 的种子编写（每桶 10 条，格式见 `evals/frozen/README.md`）。
